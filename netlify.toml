[build]
  command = "npm run build"
  publish = "dist"

[context.production.environment]
  # IMPORTANT: Set your GEMINI_API_KEY in the Netlify UI (Site settings > Environment variables)
  # GEMINI_API_KEY = "your_api_key_here"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
